# 安全策略（Security Policy）

## 支持范围

本项目为自托管开源软件，安全修复仅面向默认分支（`main`）上的最新代码与最新 Release。使用旧版本时，请先升级到最新版本，确认问题仍然存在后再报告。

## 报告漏洞

**请勿在公开 Issue、Discussion 或交流群中披露任何安全漏洞细节。**

优先使用 GitHub 的私密漏洞报告（Private Vulnerability Reporting）：进入本仓库的 **Security** 标签页，如果显示 **Report a vulnerability** 入口，直接在该入口私密提交即可。

若该入口暂未开启，请新建一个 Issue，仅注明「安全问题，需要私密联系」，**不要在 Issue 中写任何漏洞细节**；维护者会与你建立私密沟通渠道后再接收完整报告。

报告中请尽量包含：

- 受影响的版本或 commit，以及部署方式（Docker Compose / 源码构建）
- 漏洞类型与影响面（如未授权访问、注入、越权、信息泄露等）
- 可复现的步骤或最小化 PoC
- 你认为可行的修复方向（可选）

请不要在报告中附带真实密钥、真实用户数据或生产环境信息；复现材料请使用测试数据。

## 处理流程

- 维护者会尽快确认收到报告（通常在 7 个工作日内）。
- 确认为有效漏洞后，会根据严重程度安排修复与发布；修复发布后视情况进行公开说明。
- 在修复发布之前，请勿公开漏洞细节（协调披露）。如你愿意，修复说明中会对报告者致谢。

## 范围说明

以下情况一般不视为本项目的安全漏洞：

- 部署者自身的配置问题：弱密码、公开的 `.env`、长期开启 `DB_SYNC=true`、未启用 HTTPS、CORS 全放开等（参见 README 的部署安全提示与[部署文档](docs/DEPLOYMENT_AND_CONFIGURATION.md)）
- 上游模型服务、支付渠道、短信邮件等第三方服务自身的问题
- 需要超级管理员权限才能触发、且与权限设计一致的行为
- 针对部署实例的拒绝服务压测

安全测试仅允许在你自己部署的实例上进行，请勿测试他人的线上实例。
